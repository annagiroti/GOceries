## Setup Instructions

1. Clone the repository
2. Run `npm install`
3. Create a `.env` file in the frontend folder
4. Add your Spoonacular API key:
```
   REACT_APP_SPOONACULAR_API_KEY=your_key_here
```
5. Get a free API key from https://spoonacular.com/food-api
6. Run `npm start`
